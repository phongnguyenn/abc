import styled from "styled-components";

const FiltersTabbedWrapper = styled.div`
  background-color: #ffffff;
  width: 800px;

  .optionsContent {
    text-align: center;
  }

  .tabSwitch {
    float: left;
    width: auto;
    padding: 15px;
  }

`;

export default FiltersTabbedWrapper;